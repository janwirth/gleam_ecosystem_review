# Workflow & Quality Standards

## Article Update Standards

When manually editing articles (e.g., adding repos, refreshing data), update workflow files to reflect discovered nuances:

### Data Quality

- **Snapshot dates**: Update article snapshot date when repos are re-evaluated. Use ISO format (YYYY-MM-DD).
- **Exact metrics**: Stars, issue counts, commit dates must come from live GitHub web UI, not inference.
- **Date precision**: Maintenance date uses ISO format + emoji showing recency vs snapshot date (🟩🟩 same day/recent, 🟩 weeks to months, 🟥 years).
- **Activity emoji**: Compare latest commit date to snapshot date. Recent = 🟩🟩, months old = 🟩, years old = 🟥.

### README Assessment

- **Batteries-included frameworks**: Full guide + clear feature list = 🟩🟩 (not just 🟩).
- **Tagline-only**: Just description = 🟩 for clarity, 🟥 if no substance.
- **Adapter patterns**: Lightweight READMEs for single-purpose repos (e.g., "HTTP server for Cowboy") = 🟩 if clear.
- **Example repos**: GitHub-generated templates = 🟩 (shows intent, not execution).

### Issue Tracking

- **Zero issues**: Positive signal. Note explicitly in table and notes section.
- **Unknown issues** (⬜): Page load error or access denied. Document in notes.

### Table Updates

When adding repos:
1. Add to "Repos Reviewed" list (maintain original order or sort by category).
2. Add column to table (right-aligned new repos for easy review).
3. Recalculate all rows (don't assume old emoji rankings still hold relative to new entrant).
4. Update Notes section with new repo's edge cases or highlights.

## Workflow File Updates Trigger

Update `/workflows/` files when articles reveal:

- **New scoring dimension**: E.g., if "zero issues" becomes a strong signal across multiple reviews, add explicit callout in AGENTIC_REVIEW_AGENT.md.
- **New edge case**: E.g., "batteries-included framework README" wasn't differentiated before glimr. Document in REVIEW_METHODOLOGY.md under README assessment.
- **New checklist item**: If manual review surfaced a missed step, add to REVIEW_CHECKLIST.md.

### Example: Glimr Addition (2026-04-13)

Added `glimr-org/glimr` to Gleam Web Frameworks article. Insights:

- **README 🟩🟩 signal**: "Batteries-included framework" with comprehensive guide → refined README maturity assessment in workflows.
- **Zero issues signal**: Noted explicitly; considered positive maintenance/quality signal beyond just activity date.
- **Snapshot date bump**: Moved from 2026-03-22 to 2026-04-13. Workflow reminder: snapshot date is not "creation date" but "evaluation date".

### Example: shork + glimr db Addition (2026-04-26)

Added `ninanomenon/shork` (MySQL driver) and `glimr-org/framework/src/glimr/db` (framework-bundled DB layer) to Gleam Databases article. Insights:

- **Frozen-mirror edge case**: shork's README announces the project moved to Codeberg; the GitHub repo is now a stale mirror. Decision: score the GitHub repo on its frozen state, add a `[!CAUTION]` callout, and footnote the leaderboard row. Codified in REVIEW_METHODOLOGY.md.
- **Framework-bundled modules**: glimr's `db` module is the closest thing to an ORM in Gleam, but only consumable inside a glimr application. Decision: include in leaderboard with a `†` footnote flagging it as framework-bundled (not standalone), retain framework metadata for scoring. Codified in REVIEW_METHODOLOGY.md.
- **New dialect icon**: Adding a MySQL driver triggered extending the dialect legend (🐘 / 🪶 / 🐬). Updated cake (now 🐘🪶🐬) and sqlode (now 🐘🪶🐬) inline tables to reflect the third dialect they already support.
- **Disabled Issues tab**: shork's GitHub Issues UI is disabled (issue tracking happens on Codeberg). Marked as ⬜ in the issues row with explanatory note — distinct from "0 open" (positive signal).
- **Capped gleam_stdlib**: shork's `< 1.0.0` cap mirrors the migrant disregard reason. Did not disregard shork because (a) user asked to add it, (b) cake/sqlode wire to it as the canonical MySQL backend; instead flagged the cap as 🟥 in compat row and called it out in the leaderboard summary.

### Example: Parsers & Generators Article (2026-04-26)

Created `gleam/parsers-and-generators.md` covering issue #3, merging two prior OpenAPI articles into it. Insights:

- **Article merge pattern**: When sub-todos explicitly authorise it, merge narrowly-scoped articles into a broader one rather than keeping separate stubs. Preserve **all** substantive content (feature tables, code examples, gap analyses) — fold them into appropriately-scoped sub-sections of the new article. Delete the old files and update every cross-link in the codebase.
- **Multi-category leaderboard**: When an article spans heterogeneous tool classes (e.g., parser combinators vs SQL→Gleam codegen), produce **per-category leaderboards** rather than one ranking. The 7-dim score is comparable within a class, not across (a SQL codegen with 600★ shouldn't dominate the parser-combinator winner with 80★ purely on community size).
- **Cross-reference instead of duplicate**: For repos already deeply covered elsewhere (e.g., squirrel/sqlode in `databases.md#sql-code-generators`), link to the existing entry rather than re-listing the full table. Use the cross-link pattern `[squirrel](databases.md#squirrel-)` (note the trailing `-` because the source heading is `#### squirrel 🐘`).
- **Cross-link role disambiguation**: When the same repo class fits two articles (e.g., per-language lexers fit both `syntax-highlighting.md` and a parsers article), pick the **role-defining** article and have the other cross-link with a one-sentence note explaining the role-difference ("parsing for AST/analysis" vs "tokenising for colourisation"). Don't duplicate.
- **Renamed/redirected repos**: `daniellionel01/sqlc-gen-gleam` redirects to `daniellionel01/parrot` (renamed). Use the **current** repo URL in the article, mention the prior name in prose (`"renamed from sqlc-gen-gleam"`), so readers find it from either name.
- **Self-hosted Git hosts gated by Anubis** (e.g., liten.app/krig/mork): WebFetch returns the access-denied page. Mark stars/license/dates as ⬜ with a note. Don't disregard — the package still exists on Hex.
- **Unknown licenses**: When repo sidebar/LICENSE doesn't render via WebFetch but the project is real, score as 🟨 unknown (not 🟥) and add a footnote inviting the reader to check directly. Don't pretend to know.

### Example: Parsers & Generators folder split (2026-04-26)

Split `gleam/parsers-and-generators.md` into a folder `gleam/parsers-and-generators/` with sub-articles `parse.md`, `decode.md`, `generate.md`, `serialize.md`, plus a `README.md` index. Insights:

- **Operation-axis split**: When a single article spans heterogeneous operation classes (parse vs decode vs emit-code vs encode), splitting by **operation** (what the tool *does*) rather than by **input type** (what it *consumes*) keeps each sub-article internally consistent. The runtime-vs-build-time and structure-extraction-vs-emission axes naturally produce four files.
- **Per-article discovery**: Each sub-article gets its own Discovery section listing the packages.gleam.run search queries used for *that operation*. Different operations have different keyword surface (e.g. `decode` is mostly silent on `parser`, `generate` needs `codegen` + `swagger` + `protobuf`). Snapshot date repeated per file.
- **Cross-cutting themes via shared README**: When sub-articles share a rubric (the 7-dim score), document the rubric in the folder `README.md` once and link from each sub-article via `[How scores are calculated →](README.md#scoring-rubric-shared)`. Avoids duplicating the legend across 4 files.
- **OpenAPI spans three files**: `parse.md` (oas reads specs), `generate.md` (oaspec/gilly emit Gleam), `serialize.md` (Gleam→OpenAPI gap). Cross-link aggressively rather than duplicating; use anchor links like `[oaspec](generate.md#oaspec)` from neighbouring files in the same folder.
- **Cross-language framing for context, not for review**: When users explicitly ask for "other languages" framing, add a *Cross-language framing* section comparing approaches (serde / Jason / encoding-json / pydantic / utoipa) but do **not** score those tools. The article remains Gleam-scoped; the framing is for contextual orientation.
- **Per-format packages, per-direction sub-articles**: JSON / CBOR / Protobuf packages tend to bundle encode + decode in one library. The split-by-direction sub-articles cross-link to each other for the inverse side rather than re-listing packages. `decode.md` lists `gleam_json`; `serialize.md` cross-links with a note that the encoder is the same package.
- **Gap framing**: The Gleam→OpenAPI gap moves to `serialize.md` because it's about emitting a *spec document*, which is structurally a serialization (typed value → output document) rather than a code generation (input → emitted source). Surface it loudly with `[!IMPORTANT]` callouts in both `serialize.md` and the folder `README.md`.

### Example: Parsers & Generators folder unsplit + reaxis (2026-05-07)

Refactored `gleam/parsers-and-generators/` (4 files: `parse.md`, `decode.md`, `generate.md`, `serialize.md`, plus `README.md` index) into 3 articles directly in `gleam/`:

- `gleam/parse-and-generate-gleam.md` — Gleam source parsers (glance, glance_printer) + Gleam-emitting Gleam DSLs (gleamgen, trick, glue, derived).
- `gleam/parse-and-generate-other-languages.md` — parser combinators, format parsers, HTML, OpenAPI parser (oas), X→Gleam codegen (squirrel/parrot/marmot/sqlode/oaspec/gilly/oas_generator/squall/embeds), gRPC/Protobuf gap.
- `gleam/serialize-and-deserialize.md` — encoder/decoder convention, hand-written ser/deser, per-format packages, JSON Schema/Patch/RPC, bidirectional schemas, codegen for ser/deser (json_typedef, gserde, glerd_json, derived, aide_generator), Gleam→OpenAPI gap.

Insights worth codifying:

- **Folder split-by-operation can over-fragment**: The previous 4-file split (parse / decode / generate / serialize) cut along orthogonal axes (runtime vs build-time, structure-extraction vs structure-emission) that didn't cleanly partition the package landscape. Tools showed up in 2-3 sub-articles via cross-links because the axes overlapped. Re-axising by **whose language is being parsed/generated** (Gleam vs other) plus a separate ser/deser article produced cleaner per-article scope and fewer cross-cuts.
- **Articles directly in topic folder beats sub-folder index**: When a topic produces 3-4 sibling articles, putting them directly in the topic folder (`gleam/foo.md`, `gleam/bar.md`) is more discoverable than a sub-folder with a README index (`gleam/baz/README.md` + `gleam/baz/foo.md`). The folder/index pattern adds a navigation step without earning it.
- **Shared scoring rubric belongs in the canonical article, not a folder README**: When you delete a folder index, the shared scoring rubric it held has to move. Inline-and-duplicate is wrong; better is to point all sibling articles at the **first article that introduced the rubric** (`databases.md#scoring-dimensions` here) as the canonical source — the rubric is stable across articles, so one citation is enough.
- **Cross-link rewrite during refactor**: Every external article that linked into the old folder needs updating. Search pattern: `parsers-and-generators` (the folder name) catches almost all of them. Don't forget the top-level `README.md`, the `gleam/README.md` index, and CLAUDE.md (workflow rules referencing old paths).
- **Snapshot date discipline during refactor**: Re-use the most recent existing snapshot date for content that didn't change, and set the **refactor date** as the snapshot for the merged articles. Mixing 2026-04-26 (old parse.md) and 2026-05-07 (refactor day) in the merged file is a smell — use one date for the article as a whole.

## Future Article Updates

When adding to existing articles or creating new ones:
1. Use current date as snapshot (not article creation date).
2. Follow AGENTIC_REVIEW_AGENT.md process exactly.
3. Capture exact metrics from live GitHub UI.
4. If new repo type or pattern emerges, note in workflows immediately.
5. Re-rank all repos when adding new ones (don't append to old ranking).
6. Update Notes section with summary of any new repos or edge cases.

### Example: Databases article — marmot + parrot + Disregarded hoisted (2026-04-27)

Three changes to `gleam/databases.md`:

- **Add `marmot` (SQLite SQL→Gleam codegen)** — squirrel-inspired, brand new (Apr 2026), 2★, MIT, last commit 4 days before snapshot. Placed under SQL Code Generators (not migrations, not driver).
- **Re-evaluate `parrot` in proper category (SQL Code Generators)** — previously only mentioned in a footnote-style NOTE under MySQL Drivers ("not a driver, worth follow-up"). Now fully scored: 207★, Apache-2.0, sqlc plugin, multi-dialect (PG/SQLite/MySQL). Score **9** (ties with squirrel + sqlight).
- **Hoist `Disregarded` from a sub-subsection at the bottom to a top-level `## Disregarded` section after Research Method.** Rationale: when an article accumulates disregarded packages across multiple categories (was just SQLite migrations, now also `gmysql` from MySQL drivers), keeping them in a leading top-level section makes the negative-signal corner discoverable in one place — readers can immediately see "this corner has been considered, here's what was skipped and why" before drilling into per-category leaderboards.

Insights worth codifying:

- **Repo moving out of disregarded/footnote**: When a repo previously dismissed in a NOTE turns out to be in scope for a different category, **do a full 7-dim review in the right category** — don't half-measure with another note. Update the original location's NOTE to point to the new full review (so search-by-text still finds the breadcrumb).
- **Section reordering rationale (Disregarded at top vs bottom)**: Bottom = "appendix" feel (reader has to know to look). Top = "we considered the corner, this is what's not worth pursuing" (informs the reader before they read recommendations). Top placement is preferred when the disregarded list spans multiple categories. Keep the per-category leaderboards/comparisons clean of dead-end clutter.
- **Cross-article codegen entry consistency**: parrot/marmot are reviewed in **both** `gleam/databases.md#sql-code-generators` (database lens) and `gleam/parse-and-generate-other-languages.md#sql--gleam` (codegen lens). The 7-dim score should match between the two if the underlying metrics haven't drifted; add a `[!NOTE]` callout in the database article cross-linking to the codegen article (and vice-versa) so readers find the matching review either way. Do not duplicate the full code example — let one canonical entry hold the example and have the other cross-link.
- **Tied leaderboard ranks**: When new entrants tie at a score that already has multiple holders (e.g. parrot ties with squirrel + sqlight at 9), use the same award emoji for all (here: 🥈 × 3) and bump the next-ranked entry's position number to match the count above (3 entries at rank 2 → next is rank 5). Don't fudge the math by giving "2nd place" only to the first one read.

### Example: OpenAPI re-split + serialization folder + deriv flag (2026-05-07)

Three related moves in the parsers/codegen/ser-deser corner:

- **Carve OpenAPI back out into `gleam/openapi.md`** — the OpenAPI parsing section, OpenAPI → Gleam codegen (oaspec / gilly / oas_generator), and the **Gleam → OpenAPI gap** (previously living inside `serialize-and-deserialize.md`) all moved into a single dedicated article. The motivation: OpenAPI-shaped questions ("does Gleam have an oapi-codegen equivalent?") are easier to answer when the three sub-stories (parse, spec→Gleam, Gleam→spec) live together. The previous merge (commit 8a9abfa) had folded them into the broader X→Gleam article, but that diluted both articles. `parse-and-generate-other-languages.md` now cross-links instead of hosting the OpenAPI sections.
- **Folder `gleam/serialization/` for the ser/deser article** — `gleam/serialize-and-deserialize.md` moved to `gleam/serialization/serialize-and-deserialize.md`, with a new `gleam/serialization/README.md` holding the **intro / explainer prose** (what is ser/deser, why hand-write, what's in the folder). This is a deliberate **reversal** of the "articles directly in topic folder beats sub-folder index" rule from the 2026-05-07 unsplit — the difference here is that the folder is *expected to grow* (per-format sub-articles, codegen-deep-dive sub-articles), and a README is the right entry point when the folder will hold siblings rather than a single article.
- **Flag `deriv` as broken on new projects** — added a new entry under Codegen for ser/deser with a `[!CAUTION]` callout: `bchase/deriv` v2.0.0 pins `gleam_json >= 2.2.0 and < 3.0.0`, but current `gleam_json` is **3.1.0**. `gleam add deriv` fails immediately in any project depending on `gleam_json 3.x`. Verified by reproducing the error in a fresh project. Score dropped to **1** (last in the codegen leaderboard) until upstream bumps the constraint.

Insights worth codifying:

- **Article moves: the "hold the cluster" test**: When a topic spans multiple sub-stories that readers will *typically pick together* (e.g. OpenAPI parsing + spec→Gleam codegen + Gleam→spec gap), give it a dedicated article even if each individual sub-story would otherwise be a section in a broader article. The "should X live with Y" question is answered by *who is reading* — an OpenAPI user wants all three together; a generic codegen reader does not need them.
- **Folder-with-README is right when the folder will grow**: The previous unsplit deleted folders that held one or two siblings each (with the README serving only as an index). Folders earn their navigation step when they hold **3+ siblings** or are *expected to grow* (per-format ser/deser sub-articles, e.g.). A folder with a single article and a README is a tax. A folder with intro + multiple articles pays off.
- **Reversing prior-codified guidance**: When user instruction explicitly contradicts a prior rule in CLAUDE.md (here: "articles directly in topic folder beats sub-folder index" → "make a folder for serialization"), follow the user, then **codify the qualifier** that distinguishes the new case — don't just delete the old rule. Both rules are right under different conditions; the qualifier is "is the folder expected to grow with siblings?".
- **Verify "broken" claims by reproducing**: When the user says a package is "broken with new projects bc of outdated deps", reproduce the failure in a fresh `gleam new` project before writing the note — and capture the **exact error message**. The reader who encounters this resolution failure tomorrow should be able to grep for the error text and find the article. Without reproduction, the note is a rumour. With it, the note is documentation.
- **Cross-link rewrite checklist on a folder move**: When `gleam/foo.md` becomes `gleam/foo-folder/foo.md`, every external reference to `gleam/foo.md` needs `gleam/foo-folder/foo.md`, and every internal sibling reference (`bar.md`) needs `../bar.md`, and every upward reference (`../baz.md`) needs `../../baz.md`. Anchor fragments survive the move (`#section-id` still works because the heading didn't change). Search pattern that catches all three: the file's basename. Don't forget the `gleam/README.md` index, the top-level `README.md`, and CLAUDE.md.

### Example: serialization article split into 3 siblings (2026-05-07)

Split `gleam/serialization/serialize-and-deserialize.md` (the canonical ser/deser article) into 3 sibling articles in the same folder:

- `gleam/serialization/codegen-json.md` — build-time codegen for JSON ser/deser: json_typedef, gserde, sara, glerd_json, derived, aide_generator, deriv (with `[!CAUTION]` for the `gleam_json 3.x` conflict).
- `gleam/serialization/runtime-bidirectional-json.md` — runtime JSON tools: per-format JSON (gleam_json, jackson, juno, simplejson) + JSON Schema/Patch/RPC (castor, jscheam, sextant, squirtle, pollux) + bidirectional schemas at runtime (convert, kata, json_blueprint).
- `gleam/serialization/other-formats.md` — non-JSON encode/decode: CBOR (gleebor), MessagePack (gmsg), BSON (bison), Protobuf (protozoa), NBT (nbeet), LEB128 (gleb128), Base32 (thirtytwo), Base62 (sixtytwo), TOON (toon_codec), wire-protocol codecs (postgresql_protocol, h2_frame).
- `gleam/serialization/README.md` — gateway article: encoder/decoder convention, hand-written ser/deser, cross-language framing, links to the 3 siblings. Replaces `serialize-and-deserialize.md` as the canonical entry-point.

Insights worth codifying:

- **Codegen vs runtime vs non-JSON is the right 3-axis split for ser/deser**: The package landscape partitions cleanly along (a) build-time codegen vs runtime libraries, (b) JSON vs everything else. JSON has the deepest tooling so it gets a build-time and a runtime article; non-JSON formats are siblings sharing a runtime-encoder shape. A 4th axis (e.g. per-binary-format) was tempting but would over-fragment — every binary format has one Gleam package, so they share an article.
- **Foundational content lives in the folder README, not duplicated in siblings**: The encoder/decoder convention, hand-written examples, and cross-language framing belong in the gateway README. Siblings are catalogues of tools; the README is the introduction. Each sibling cross-links back to `README.md#the-encoderdecoder-convention` rather than re-introducing the pattern. Avoids duplication; readers always land on the convention from any entry-point.
- **Anchor migration on a file split is not automatic**: When `serialize-and-deserialize.md#json_typedef` becomes `codegen-json.md#json_typedef`, every external link to the old anchor must be re-pointed. Anchors don't migrate automatically. Build a one-time map (old anchor → new file + anchor) and grep-and-replace; don't trust naive find-replace because some anchors may have moved files (e.g. `#json_blueprint` moved from the bidirectional section to `runtime-bidirectional-json.md`).
- **Replacement of canonical article requires a stale-file delete**: After splitting, `serialize-and-deserialize.md` must be **deleted** (not left as a stub or redirect). Stub files create stale URLs and grep noise. The folder README absorbs the article's gateway role and points to the 3 siblings.
- **Cross-link rewrite scope grows with article count**: Splitting one article into three means every external article that linked into the original needs to choose **which** new article to link to per anchor. The `#codegen-for-serdeser` anchor goes to `codegen-json.md`; `#json_blueprint` goes to `runtime-bidirectional-json.md`; `#protozoa` goes to `other-formats.md`. Unlike a folder move (where every link goes to the same new path), a split forces per-anchor decisions.

### Example: Hashing article (2026-05-10)

Created `gleam/hashing.md` covering ~18 packages: cryptographic (gleam_crypto, glesha, 4× SHA-3/Keccak, BLAKE2/3), non-crypto (2× Murmur3, SipHash, Knuth-multiplicative), HMAC/signing-adjacent (hypersig, hashsigs), content-addressing (multiformats, gleam-merkle, bloomfilter_gleam). Cross-links to `authentication.md#password-hashing` rather than re-reviewing the password-hashing slot.

Insights worth codifying:

- **"When to use what" decision table belongs in articles where the catalogued tools aren't substitutable**: A leaderboard ranks competing implementations of the same job (e.g., 3× Argon2 NIFs in authentication.md — they're substitutable). A hashing article is different — gleam_crypto (SHA-256), gblake3 (BLAKE3), and mumu (Murmur3) cannot substitute for each other. Add an explicit "When to use what" section keyed by **the user's job-to-be-done** (verify a download, sign a webhook, key a hash map, content-address a blob, …). The leaderboard is then for picking *between* competing impls of the *same* algorithm. Loud `[!IMPORTANT]` callout in the leaderboard reminding the reader of this distinction.
- **"What's missing" should list explicit FFI escape hatches, not just "no Gleam package"**: When a category has many holes (xxHash, CRC32, FNV, RIPEMD, PASETO all absent from Gleam), don't just say "missing." Give the **specific** Erlang/Elixir Hex package or stdlib function the reader can FFI to. The "What's missing" table becomes actionable rather than just a complaint.
- **Companion articles cross-link with a guard-rail callout**: When the new article overlaps a slot already covered elsewhere (password hashing in `authentication.md`), don't re-review — link, **and** add a `[!IMPORTANT]` callout in both directions warning against the wrong-tool failure mode (here: "never use `gleam_crypto.hash` for password storage, never use `argus` to verify a download"). Cross-links say *where* to go; callouts say *why* the wrong choice is dangerous.
- **Multiple impls of the same algorithm need explicit differentiation in TOC + summary table + leaderboard**: When two packages do the same thing (e.g., Murmur3 — `murmur3a` pure-Gleam dual-target vs `mumu` FFI-wrapper dual-target), surface the distinguishing axis (target / impl strategy) **at every level**: in the TOC entries, in the summary table cell, and in the leaderboard "Highlights" column. Don't make the reader scroll to the per-package section to figure out why both exist. **Investigate the *why*** — read both READMEs, compare commit dates, check for cross-references — and surface the answer as a `[!NOTE]` callout. "Parallel discovery" vs "intentional alternative" vs "supersession" are all common; the reader needs to know which.
- **Don't trust an at-a-glance impl-strategy claim**: First pass on `mumu` had it as "BEAM-only NIF wrapper" because the Erlang NIF dep dominated the dependency list. Closer reading showed `mumu` also vendors `murmurhash3_gc.mjs` and ships `@external(javascript, ...)` declarations — it's dual-target via FFI on both runtimes. Always inspect both `@external` clauses (or the equivalent for the language) before claiming a target is unsupported. The `target` field in `gleam.toml` is also load-bearing — `target = "javascript"` means JS-only; absence usually means dual-target.
- **Git-only / not-on-Hex packages are still in scope**: Two Keccak impls (`keccaky`, `sha3` poulwann) live on GitHub but aren't published to Hex. They're discoverable via `language:gleam` GitHub search and may be the only path for a given algorithm. Score them like Hex packages, but flag the install friction in their description ("git-only — clone-and-build only" or "via `git`-dep in `gleam.toml`"). Don't omit them.
- **Disregarded entries can include same-named foreign-language packages**: A Hex search for `murmur` surfaces `preciz/murmur` which is **pure Elixir, not Gleam**. List it in Disregarded with the explicit "not Gleam" reason — Gleam users searching the registry will find it and need to know it's not a Gleam-native option (but is callable via FFI). Same applies for any cross-language Hex package that surfaces in keyword searches.
- **Reference the canonical scoring rubric from one article only**: hashing.md cites `databases.md#scoring-dimensions` as the canonical rubric source rather than re-stating it. Same pattern as serialization siblings. Reduces duplication — when the rubric is updated, only one file needs editing.

### Example: CLI article (2026-05-12)

Created `gleam/cli.md` covering ~39 packages across two distinct concerns: argument parsers (glint, clip, clad, hoist, argv, glap, outil, sheen, cosmo_cli) and interactive UI / CLI renderers spanning seven sub-areas (colour, spinners, prompts, tables, terminal info, full-screen TUI, exit). Cross-links to `subprocesses.md` for shelling out and `testing/general-testing.md` for test runners that surface in CLI searches. Per-category leaderboards (parsers vs UI) since they score against different magnitudes.

Insights worth codifying:

- **"Two ecosystems, one article" pattern**: A topic that splits cleanly into two **non-substitutable concerns** (an argv-parser cannot render a spinner; a TUI framework cannot parse argv) still belongs in **one article** if a reader writing a CLI typically picks one tool from each. Use a leading paragraph to declare the split, give each concern its own top-level section under Categories, and produce **per-concern leaderboards** at the bottom rather than one global ranking. Same axis-management discipline as the hashing article's "When to use what" — pick by job, not by global score.
- **"FFI escape hatch" applies beyond the hashing article**: The pattern of "list the capability gap, then point at the specific Erlang/Elixir Hex package that fills it" generalises. CLI got Owl (multi-select / password / autocomplete), Ratatouille (Bubble Tea–equivalent TUI), Optimus (completion-script generation), and `fzf` (shell out for fuzzy-find). When a category surfaces 5+ capability gaps, the "What's missing" table earns its keep.
- **Sub-area thinness within an article**: The interactive-UI section has 7 sub-areas (colour, spinners, prompts, tables, terminal info, TUI frameworks, pretty-messages) where most contain only 1-3 packages. Use **smaller per-sub-area summary tables** (3-5 columns max) instead of one mega-matrix that conflates 7 different concerns. The mega-matrix at the bottom (Comparison matrix) consolidates all 39 packages on the canonical 7 dimensions for reviewers who want to compare globally.
- **`target = "javascript"` in `gleam.toml` is not always the full story**: `etch` declares `target = "javascript"` but its README documents both JS (Bun/Node/Deno) **and** Erlang (via Gleescript / Burrito) deployments. Always read the README's deployment section in addition to the `target` field. The `target` field declares what the *package author tested against*; FFI bindings can extend reach. Conversely, `pink` declares `target = "javascript"` and the README confirms JS-only because Ink itself is a Node library — the field is accurate there.
- **Repo-deleted-but-Hex-still-published edge case**: `cosmo_cli` is on Hex (last release 2025-05-08, 235 downloads) but its GitHub repo returns 404 — the author appears to have deleted the source. Decision: still list it for discoverability (Hex search will find it), score most dimensions as ⬜, mark the row "n/a" in the leaderboard, and add an explicit "**avoid for new code** — broken upstream signal" callout. Distinct from "stale" (low maintenance signal but reproducible source) and "Anubis-gated" (source exists but tools can't reach it).
- **Two-Ink-bindings parallel discovery**: Same pattern as Murmur3 in the hashing article — `pink` (Massolari) and `glink` (han-tyumi) both wrap Node's Ink, both target JS-only, neither cross-references the other in their README. Codified the pattern in hashing.md as "investigate the why and surface the answer as a `[!NOTE]`" — applied here verbatim ("looks like parallel discovery rather than supersession or competition").
- **GitLab repos surface in Gleam too** (`string_width` lives at `gitlab.com/arkandos/string-width`). GitLab's web UI doesn't expose the star count as prominently as GitHub; the `gh api` shortcut doesn't work. Decision: score what's available (license, recency, README), mark stars as ⬜, note the hosting in the description. Don't pretend the gitlab star count and the github star count are the same dimension — they aren't.
- **The "GPL in a category with no MIT alternative" callout pattern**: `glitzer` is the **only** Gleam progress-bar library and it's GPL-3.0. This is qualitatively different from a GPL-licensed package competing against MIT alternatives (where you'd just pick the MIT). When the GPL package is the only option in its slot, the article needs a `[!WARNING]` callout in the per-package section **and** a row in "When to use what" explaining the workaround ("render progress bars yourself with cursor moves") for proprietary distribution.
- **"License missing" is a real signal in tiny-author-personal-project ecosystems**: ~12 of the 39 packages reviewed had no license declared. In ecosystems where most packages are <50★ side projects, "no license" is the modal case rather than the exception. Score it 🟥 consistently, but don't disregard the package on that alone — surface the "no license" signal in the per-package note and let readers triage. (Unlicensed code is technically "all rights reserved" — reusable only if the reader is willing to email the author.)

### Example: Caching article (2026-05-14)

Created `gleam/caching.md` covering ~24 packages across seven categories: memoisation (5 packages), in-memory caches & state cells (6), ETS bindings as foundations (6), persistent/disk-backed (4), out-of-process (2 + 1 cross-link), HTTP/web framework (1 built-in + 1 deprecated), domain-shaped (3). Includes a "Background: caching primitives across targets" section explaining ETS / `persistent_term` / Mnesia / DETS on BEAM vs `Map` / `WeakMap` / `Cache API` on JS. Cross-links to `databases.md` (for `valkyrie` Redis client), `hashing.md` (for content-addressing CIDs), `authentication.md` (for signed cookies).

Insights worth codifying:

- **"User flagged as bad" requires reproduction, not deference**: The user's todo said *"carpenter and glemo are bad"* — the right move was to reproduce the failure in fresh `gleam new` projects, capture the **exact** build error (`The module gleam/dynamic does not have a from value`), and trace the root cause (carpenter's `gleam_erlang ~> 0.24` pin pulls in `gleam_erlang 0.34.0`, whose source references `dynamic.from`, which was removed in `gleam_stdlib 1.0`). The `[!CAUTION]` callouts in the Disregarded section now name (a) the file and line of the offending source, (b) the version-resolution chain that produces the conflict, (c) the workaround if the reader is stuck with an older project, and (d) the modern replacements. Future articles flagging "broken on new projects" packages should follow the same depth — error message + chain + workaround + replacement, not just "it's broken."
- **Articles for foundational primitives need a Background section even if the article is Gleam-scoped**: Caching depends on understanding ETS, `persistent_term`, Mnesia, DETS, `Map`, `WeakMap`, and the Cache API — primitives the reader may know from Elixir/Erlang/JS but not from Gleam's framing. Added a "Background: caching primitives across targets" section explaining each primitive's trade-offs (process-owned vs named tables, public/protected/private, set/bag/ordered_set, no TTL natively, no LRU natively, persistent_term global-GC cost, etc.) before reviewing any package. This is **not** the same as a "Cross-language framing" section (which compares ecosystems for the reader's contextual orientation) — it's a "Background" section explaining the **primitives the Gleam packages wrap**. Pattern: when most of the reviewed packages are thin wrappers around a small set of underlying primitives, surface those primitives explicitly before package reviews. Otherwise the per-package descriptions become noise.
- **"There is no X" framing as a guard rail**: Caching has no Gleam Cachex / Nebulex equivalent. Three of the article's strongest sentences are *negative claims* — "**There is no Gleam `cachex`**", "**No TTL primitive** anywhere except [...]", "**No LRU on ETS**." Don't bury negative findings in the "What's missing" table. When the negative finding **shapes the recommendation**, surface it in the State-of-the-ecosystem paragraph too. Readers searching for "Gleam cache with TTL" need to hit the wall fast and either pivot (Redis / Elixir Cachex FFI) or accept the DIY cost — not scroll through six paragraphs of optimism before finding out.
- **`gleam_stdlib` constraint as a leading indicator of broken-on-new-projects**: A `gleam_stdlib < 1.0.0` constraint (like bravo's `>= 0.58.0 and < 1.0.0`) is a near-guarantee of breakage on a current toolchain — the package was written against pre-1.0 stdlib and either the resolver downgrades stdlib (risky for the rest of the project) or fails outright. Same root cause as carpenter / glemo. When you see `< 1.0.0` on stdlib in a package's `gleam.toml`, treat it as a 🟥 in the compat dimension and **reproduce the install** to see what happens; don't just trust the constraint. (Bravo was the surprise — its surface looks healthy at 30★ + Apache-2.0 + comprehensive README, but the dep constraint silently makes it broken in the same way as carpenter.)
- **Transitive-dep breakage propagates predictably**: `glemo` is broken because `carpenter` is broken. When package A depends on broken package B, package A is broken transitively — even if A's own constraints look fine. Always check the **transitive** dep chain when reproducing a build failure, not just the direct deps. Document the chain in the Disregarded entry so future readers (and the resolver-confused future-self) can trace it.
- **"Deprecated by author with redirect to non-equivalent package"**: `functx` (caching context) was archived on GitHub and the README redirects to `klubok-gleam` — which turns out to be a **testing/mocking library**, not a cache. The author conflated two unrelated patterns under similar names. Decision: list `functx` in Disregarded with the redirect *and* the warning that the redirect leads somewhere else; don't follow the redirect blindly when summarising. Pattern: when a deprecation notice redirects readers to another package, **verify** the target package actually does the thing the deprecated one did, and surface the mismatch if it doesn't.
- **"Upstreamed into the framework" is a real deprecation reason**: `cachmere`'s HTTP caching was upstreamed into Wisp via [PR #113](https://github.com/gleam-wisp/wisp/pull/113); the standalone package is no longer maintained but the feature lives on inside Wisp. Document the **destination** of upstreamed features prominently so readers searching for "Gleam HTTP cache" find the Wisp built-in rather than the dead package. Cross-link from Disregarded + from "When to use what" to the Wisp doc, not to the deprecated package.
- **Cross-target primitive availability tables earn their keep**: Added a 3-column "Need / BEAM only / Dual-target / JS only" decision table in the Background section, classifying every reviewed package by target-availability per use case. Readers writing dual-target code can scan one row and find the dual-target option (`memo_gleam`, `booklet`, `capuchin_crypt`, etc.) instead of reading every per-package section to figure out the target. Applies wherever target-availability is a first-order decision criterion (caching, hashing, serialisation, environment vars).
- **Domain-shaped caches deserve their own subsection**: Caches with a domain-specific API (TanStack-Query-shape SWR, Relay-shape GraphQL, Lustre vdom `element.memo`) don't compete with general-purpose key/value caches — they live in a different evaluation framework (their value is *the domain pattern*, not the cache mechanics). Carved them into a separate "Domain-shaped caches" subsection rather than mixing them into "general caches." Pattern: when a topic has both **general** tools (substitutable across many use cases) and **domain-shaped** tools (only useful for one specific shape), split them. Score both, but rank them within their own subsection.
- **Pure-Gleam packages often `target = ""` (i.e. unset)**: Worth confirming that "no target field in gleam.toml" usually means dual-target — there's no `target = "javascript"` on most pure-Gleam caches. When the target field is **absent**, double-check by reading the source for `@external(erlang, ...)` and `@external(javascript, ...)` clauses to see what FFI is actually used. (For caching specifically, the patterns are: pure-Gleam = dual-target trivially; ETS uses = erlang-only by physics; mixed FFI = read both `@external` clauses.)

### Example: Root-folder almanac restructure (2026-06-15)

Folded ~12 cross-ecosystem articles out of the repo root into 4 thematic folders to support the almanac framing. Loners (`authentication.md`, `seo-and-analytics.md`, `postman-to-openapi-converters.md`) stayed at the root because they don't have siblings yet.

- `application-types/` — `README.md` (was `application-types.md` — kept as the folder index because it's an overview that *already* points at sibling per-type articles), `desktop.md` (was `building-desktop-apps.md`), `mobile.md` (was `building-mobile-apps.md`), `browser-ssh-terminals.md`. 4 siblings.
- `design/` — `README.md` (new — folder index), `ux.md` (was `ux-resources-and-tools.md`), `diagramming.md`. 2 siblings + intro.
- `practices/` — `README.md` (new — folder index), `bdd-with-gherkin.md`, `domain-driven-design.md`, `formalization.md` (the scoring rubric — used by every review). 3 siblings + intro.
- `industry-watch/` — `README.md` (new — folder index), `navigating-ecosystems.md`, `language-popularity.md` (was `programming-languages-popularity-and-desire.md` — shortened), `recent-incidents.md` (was `recent-incidents-in-major-technologies.md` — shortened). 3 siblings + intro.

Insights worth codifying:

- **The "almanac" structure: thematic folders + loner-root rule**: An almanac is organised by topic, not by chronology or alphabet. Cluster articles into folders by *what question the reader is bringing* — "what should I build?" (`application-types/`), "how should I think about it?" (`practices/`), "what's happening in the industry?" (`industry-watch/`), "how should it look and communicate?" (`design/`). Folders are right when **≥2 siblings exist** *and* an overview article (the folder `README.md`) can justify the navigation step. Sole articles in a topic stay at the root — a single-article folder is a tax (rule confirmed from the 2026-05-07 serialization-folder example).
- **Folder-index from overview vs new-write**: When an existing article is already an *overview that points to sub-articles* (here: `application-types.md` listed mobile + desktop + PWA), rename it to `<folder>/README.md` — it already does the folder-index job. When no such article exists, write a *short* (≤30-line) new README: 1-paragraph "what's in this folder", bullet list of siblings with one-line descriptions, "Related" section to neighbouring folders. Resist writing a long intro — the README's job is navigation, not exposition.
- **Renamed-on-move = shortened slug**: When a file moves into a folder whose name already conveys context, the file basename can shed redundant words. `programming-languages-popularity-and-desire.md` → `industry-watch/language-popularity.md` (the "programming languages" framing is implied by the folder; `-and-desire` is redundant with the article's own framing). Same for `recent-incidents-in-major-technologies.md` → `industry-watch/recent-incidents.md`. The shorter slug compounds with the folder name to read naturally in cross-link paths.
- **Cross-link rewrite at scale: script it, then verify with a link-walker**: 20 files needed cross-link rewrites across 12 renames. Hand-editing 20 files invites typos. Python script: build a `{old_basename: new_full_path}` map; for each source file, regex-find `\]\(((?:\.\./)*)(basename)(#anchor)?\)`, compute `os.path.relpath(target_abs, source_dir)` for each match, substitute. Then a verification pass: walk every `.md`, parse every `](X)` link, resolve it against the source file's directory, assert it exists. Caught two classes of bug the regex missed — (a) bare-basename references to root *loners* (`authentication.md` was bare in moved files; now needs `../authentication.md`), (b) root-relative paths to *non-moved* folders (`gleam/...`, `workflows/...` need `../` prefix from moved-file locations).
- **URL-encoded paths look broken to grep but resolve**: The link-walker reported `./Notion%20API.postman_collection.json` as broken because `os.path.exists` doesn't URL-decode. Add a `urllib.parse.unquote` step to the verifier so false positives don't drown out real breakage.
- **Loner rule explicit**: The empty space between "folder of 3+ siblings" and "article at the root" is "folder with 1 article + README" — which is a tax (per 2026-05-07). The corollary: when a topic *only has one article today*, do not pre-emptively create a folder for it. Leave the article at the root. When the second sibling lands, *then* create the folder and move both at once. Applied here: `authentication.md`, `seo-and-analytics.md`, `postman-to-openapi-converters.md` each could justify a `security/`, `marketing/`, `data-interop/` folder respectively — but with only one article each, the folder would just be a redirect. Wait for siblings.

### Example: SEO+analytics split into `marketing/` (2026-07-02)

Split `seo-and-analytics.md` (one root loner, ~664 lines) into a `marketing/` folder with two siblings + a folder README:

- `marketing/README.md` — folder index. Holds the shared **"SEO vs analytics line"** framing (two-disciplines-one-loop intro), snapshot date, and pointers to the two siblings.
- `marketing/seo.md` — acquisition side: keyword research, competitor, technical/on-page/off-page, GEO, tool tiers, per-capability feature comparison matrix (**15-column** across ~40 tools), "which tool for which job" decision table, playbook.
- `marketing/analytics.md` — measurement side: web vs product analytics, per-capability matrix (**18 columns** across ~26 tools), GA4, privacy/consent, dedicated `## Self-hosted PostHog — resource requirements` section with a `[!CAUTION]` callout (concrete RAM/vCPU/services numbers + Cloud recommendation).

Insights worth codifying:

- **The loner-rule trigger fires when the loner article is already two articles wearing a trench coat.** `seo-and-analytics.md` was one file, but it contained two operationally-independent audiences: **the marketer who wants to be found** (SEO) vs **the product owner who wants to measure what people do** (analytics). Different tool landscapes, different vocabulary, different budget conversations. A reader searching for "which analytics tool to buy" doesn't want the keyword-research tooling section between them and the answer. When a single loner article has this shape, splitting it into a folder is the right move even though the split *creates* the two siblings that justify the folder — the folder existing pre-populates the invitation for future marketing articles (email deliverability, paid ads, attribution, ...).
- **"Per-capability feature comparison matrix" is the right shape when the user's real question is procurement.** The user told us their problem verbatim: *"our current problem is selecting a paid tool and I want to understand what tool does what."* That's a procurement question — the deliverable is a tool→capability matrix (rows=tools, cols=capabilities, cells=✅/🟨/⬜), not a global leaderboard. Global leaderboards answer "which tool is best" (wrong question when tools aren't substitutable); matrices answer "which tool covers *my* capabilities" (right question when the user has a specific short-list to build). Pair the matrix with a **"Which tool for which job" decision table** keyed by JTBD ("I only need keyword research", "I have $30/mo", "I must self-host") so readers with narrower needs skip the matrix and land on a pick.
- **Group matrix rows by tool class when the matrix would otherwise be 40+ rows wide.** Instead of one giant table with 40 rows × 15 columns, break into sub-tables per tool class (Full platforms / Technical / First-party+free / Content / Backlink+outreach / Local / GEO / Mentions). Within each class the ✅ patterns cluster; across classes the ✅ patterns diverge, which is *itself* the signal the reader wants. Sub-tables also fit horizontally without scroll on typical screens.
- **Column shorthand + legend + notes-subsection are load-bearing when cell states are non-binary.** With 15+ columns, headers need short labels (`Web` / `Product` / `Autocap` / `Replay` / …) and a **legend** section decoding them. With 🟨 as a real state, a **notes-on-specific-cells** subsection is needed — otherwise readers can't tell "🟨 = paid add-on" from "🟨 = beta" from "🟨 = present but scope-limited." Analytics matrix used all three of shorthand + legend + notes.
- **A "user flagged X as bad" claim about a hosted service also needs sourcing, not just reproduction.** The user's todo said *"the open-source version of posthog is bad, very resource hungry"* — the analytics agent's move was to source the claim via PostHog's own self-host docs (concrete "4 vCPU / 16 GB RAM / 30+ GB SSD" starting config), the developing-locally handbook (7+ services), and a community deep-dive. The `[!CAUTION]` callout then names (a) service list, (b) idle RAM at rest, (c) recommended VM size from PostHog's own docs, (d) K8s-no-longer-supported for new paid deploys, (e) the alternative (PostHog Cloud). Same discipline as the Gleam "carpenter is broken" example but adapted for a hosted-service resource-hunger claim: source the numbers rather than restating the user's phrase.
- **Splitting an article deletes anchors — cross-link rewrite scope grows.** `seo-and-analytics.md#quick-start--...` became `marketing/seo.md#quick-start--...`; `#platform-landscape` moved to `analytics.md`. Same anchor-migration discipline as the 2026-05-07 serialization split — grep for the old basename across the repo, decide per-anchor which sibling the link should now target, and rewrite. Small enough scope here (only root README + design/README linked in) that hand-edit sufficed; would have wanted the Python link-walker script from 2026-06-15 if scope had been larger.
- **The `application-types/desktop.md` recipe pattern**: When a user asks to add a "custom-build stack" that composes existing reviewed tools (Gleam + Lustre + Burrito + Electron), it does **not** belong in the per-framework leaderboard — it's a build recipe, not a competing framework. Add it as a `## Recipe — X` section at the end of the article (before Cross-link/Discovery), with a leading `[!NOTE]` callout naming it as a recipe (not a scored framework), and add a one-sentence pointer from the closest archetype's Categories entry ("See also: Recipe — X for a Y variant of this pattern"). Structure the recipe as: Goal, Stack diagram (ASCII), Why-this-combo, Build steps (checklist), Trade-offs (real ones — bundle size, cold start, dual code-signing), When to pick vs alternatives, Cross-links. Do **not** add a row to the framework comparison table — respect the "recipe not framework" framing.

### Example: Website builders article (2026-07-27)

Created `application-types/website-builders.md` — ~60 platforms across six families (mainstream all-in-one, designer-first, portfolio/creative, WordPress + open-source, AI-first/prompt-to-site, Notion-as-CMS), plus ecommerce platforms and no-code app builders reviewed *only* to draw a boundary. Cross-linked from `application-types/README.md` (Website archetype) and the root README.

Insights worth codifying:

- **The "tooling survey for an archetype" slot**: `application-types/README.md` describes five archetypes and lists illustrative frameworks per archetype; `desktop.md` and `mobile.md` are the deep tooling surveys for two of them. The Website archetype had a framework table (Hugo/Astro/11ty) and no survey — and that table only covers people who write code, which is a minority of website-makers. **When an overview article names archetypes and only some have companion surveys, the missing ones are the article slots.** Place the survey as a sibling, add an `[!IMPORTANT]` pointer from the archetype's own section (not just Related reading), and say explicitly what the overview's table *doesn't* cover.
- **Verification failure is itself a finding, and belongs above the fold**: Wix, Squarespace, Jimdo, Square, Site123, Elementor and Builder.io all serve pricing pages whose feature matrices and transaction-fee percentages render fine but whose **dollar figures are absent from the HTML**; GoDaddy returns 403 to every automated request including its generic `/pricing`; Wix tells prospects to view Studio pricing in incognito. Rather than burying this, the article opens with a **"A warning about the prices in this article"** section plus a 4-state marking table (verified / ⬜ unknown / *secondary* / *community-reported*) applied consistently throughout. **Pattern: when a category systematically resists verification, document the resistance as a section, define the confidence markings once, and use them everywhere — don't launder listicle numbers into apparent facts.**
- **Pricing-model taxonomy beats per-tool price lists**: prices go stale in weeks; *models* don't. Leading with six models (flat per-site · two-axis site×seat · credit metering · intro-then-renewal · transaction fees · metered overage) and naming the characteristic buyer failure mode of each makes the article survive its own price drift. Per-tool numbers then hang off the model. Reusable wherever a category has heterogeneous pricing (hosting, observability, LLM APIs, CI).
- **"Behaviour at the cap" is a distinct axis from "the cap"**: every vendor publishes limits; almost none publishes what happens when you hit one. Four behaviours found — grace-then-forced-upgrade-non-refundable (Webflow), transparent metered overage (Framer, Webstudio), grace-then-cliff/site-goes-offline (Framer bandwidth), tier-forcing-with-no-overage (Wix, Squarespace). **Ask "and then what?" of every published limit and make it its own table.**
- **The export/lock-in table is the highest-value artefact in a SaaS survey**: the question with the widest variance and the one buyers ask last. The load-bearing rule discovered: ***if a builder's export excludes CMS content, the export is a mockup, not a migration*** — which reclassifies Webflow (advertised export, real HTML, empty Collection Lists) from "portable" to "you own the markup, not the machine." Rank vendors on what you *actually* own, and quote the vendor's own refusal verbatim where one exists (Framer: *"Framer does not offer HTML export for self-hosting"*; IONOS recommending **HTTrack** against your own site).
- **"By construction" vs "by configuration" is the right split for capability limits**: SEO limitations that a tier upgrade fixes are configuration; forced URL prefixes, client-side rendering, missing canonical, and platform-level index blocking are structural. A dedicated *"SEO capability by construction"* section keeps the buyer from assuming money solves it. Also produced a reputation correction worth making explicitly (Wix's JS-rendering reputation is stale — it now posts the best mobile CWV pass rate; its real constraint is the `/post/` prefix).
- **Adjacent categories deserve a boundary section, not omission**: readers actively mis-evaluate ecommerce platforms and no-code app builders against website builders. Review them *briefly*, and find the **decisive fact** rather than comparing features — here, rendering. Two vendors said it themselves (Glide: *"Glide now prevents apps from being indexed by search engines"* and recommends building a separate SEO site; Retool staff: client-side rendered, *"google just sees a blank page"*). **A vendor's own admission is worth more than any feature matrix row.** The generalised rule that fell out: *a tool whose bill grows with your readership is not a website tool.*
- **Family taxonomy replaces the global leaderboard when tools aren't substitutable**: Framer, WordPress and Lovable don't compete. Six families, per-family capability matrices, one JTBD decision table — same discipline as `gleam/hashing.md`'s "When to use what" and `marketing/seo.md`'s procurement matrix, but at a scale where the *families* also need declaring up front with an `[!IMPORTANT]` guard-rail.
- **Sub-agent research clusters map to article families, and one cluster failing is survivable**: five parallel research agents (mainstream / designer-first / OSS+WordPress / AI+ecommerce / cross-cutting-gotchas) mapped almost 1:1 onto article sections. One agent delegated half its brief to a child and exhausted its search budget, returning Cluster A only and explicitly reporting Cluster B as **not delivered** — re-dispatched as a fresh focused agent. **Instruct research agents to do the work themselves rather than sub-delegating, and to state undelivered scope explicitly rather than silently truncating.** The honest "not delivered" report is what made the recovery cheap.
- **Market-share data reframes "best tool" as "tool you can hire for"**: HTTP Archive shows Elementor at 32.67% of WordPress while the builders the internet is loudest about (Bricks 0.34%, Oxygen 0.44%, Breakdance below cutoff) hold under 1% between them, and full-site editing sits at 1.67% after four years. **Where population data exists, include it and state the "separate best tool from hireable tool" reading** — it changes recommendations more than any feature comparison.
- **Name the shutdowns that didn't happen**: research surfaced two false shutdown stories — "Universe shut down" (a K-pop fan platform of the same name, not the builder) and "Google Web Designer discontinued" (only old versions retired; the likely confusion is Ads Creative Studio's March 2025 sunset). **When a shutdown rumour is checkable and false, say so in the article** so the rumour stops propagating through the next reviewer.

### Example: Typography article (2026-09-07)

Created `design/typography.md` — vertical rhythm, type scales, fluid type, leading trim (Capsize / `text-box-trim`), metric-matched fallbacks, and the phrase "vertical rhythm reset". ~45 repos/tools across four non-substitutable categories plus a survey of what 18 design systems actually ship as type tokens. Fills the hole `design/ux.md` explicitly declared ("not visual design — typography, …").

Insights worth codifying:

- **Research agents can die on a session rate limit after partial delivery; check `/tmp` before re-dispatching.** Five parallel research agents were dispatched; all five were killed by a 429 session limit. One had written its full notes file first; the others left raw artefacts (fetched READMEs, token JSON, source files) but no summary. Reading the artefact directory recovered ~40% of the lost work. Rule: instruct agents to write their notes file *incrementally* (append per section), and on failure inventory `/tmp/research/` before paying for a second pass. The second pass here was done inline with `gh api` + `WebFetch` batches of 10–15 calls, which finished without hitting the limit.
- **"Reproduce the tool output" applies to typography libraries too.** Capsize's trims (`-0.3862em` for Inter 16/24), `createFontStack`'s `size-adjust: 107.1194%`, and `utopia-core`'s `checkWCAG` returning a `[1010, 2060]` failing viewport range for a 16→48px clamp were all generated in a fresh `npm init` project and quoted verbatim. The WCAG fail range in particular is not derivable from any docs page — only from running the code — and it turns "fluid type can fail 1.4.4" from a warning into a number.
- **Read token *source files*, not docs pages, for design-system comparisons.** m3.material.io, atlassian.design and carbondesignsystem.com are JS-rendered and returned nothing to the fetcher. The token values came from `flutter/flutter typography.dart` (Material 3, sizes × `height` reproduce the sp table exactly), `microsoft/fluentui fonts.ts`, `alphagov/govuk-frontend _typography-responsive.scss`, `carbon-design-system/carbon scale.ts` (which also has the formula as a code comment), `uswds/uswds type-scale.scss`, `Shopify/polaris font.ts`, `primer/primitives typography.json5`. `gh api -H 'Accept: application/vnd.github.raw'` on the file path is one call per system and immune to JS rendering. Where only the docs exist (Atlassian), mark ⬜ — don't quote a search-engine summary as the token value.
- **A "does the design system have vertical rhythm?" column exposes marketing.** Of 18 systems, only Every Layout and SEEK Braid derive spacing from line-height; the rest run an independent 4/8px spacing scale, and several (Bootstrap, MUI, Apple, USWDS, Primer) don't even keep line-heights on a 4px grid. The column is cheap to fill from the tokens and is the single most useful row for a reader who was told "our system has rhythm."
- **Decode an ambiguous user phrase into all plausible readings, ranked, and score the literal one.** "Vertical rhythm reset" turned out to be (1) a specific 79★ Sass repo (`jhildenbiddle/vertical-rhythm-reset`), (2) the typography rules inside modern CSS resets (Bell 2023, Comeau 2021/2026), and (3) the leading-trim family. All three are in the article, ranked by how likely a search lands on each; the literal repo is scored and placed in the leaderboard rather than being the article's spine. Don't build the article around the most literal reading of a two-word todo.
- **"Platform absorbed the category" is a maintenance-score pattern, not a failure signal.** Every 2006–2019 rhythm library (Compass, Sassline, typecsset, Plumber, typography.js) is dormant because `lh`/`rlh`, `calc()`, custom properties and `text-box-trim` do the job natively. The leaderboard therefore ranks a *token set* (Open Props) first and puts the "MVP in 12 lines of CSS" snippet above the tool tables. When the browser has eaten a category, say so in the section lead and score the survivors honestly rather than inflating a dormant library's maintenance because "nothing newer exists."
- **Score-vs-recommendation divergence should be stated, not hidden.** `utopia-core` scores 2 on the rubric (138★ helper repo, no licence field) and is the tool the article recommends for fluid type. The leaderboard entry says exactly that. Same for Sassline (scores 6, author-deprecated). The rubric measures repo health; the recommendation measures fitness; when they disagree, the reader needs both numbers and the reason.
- **Native-platform parity tables earn their keep for CSS-mechanics articles.** Compose's `LineHeightStyle.Trim` (`FirstLineTop` / `LastLineBottom` / `Both`) is the same model as `text-box-trim`; Flutter's `TextLeadingDistribution.even` is literally CSS half-leading; Android 14's non-linear font scaling is the platform version of "clamp max ≥ 2× min." One table with five rows makes the web behaviour look like an industry consensus instead of a quirk, and cross-links the article to `application-types/mobile.md`.

### Example: Web-presence stack decision article (2026-07-27)

Created `application-types/choosing-a-web-presence-stack.md` — a five-tier spectrum (off-the-shelf builder → visual development platform → SSG + git CMS → framework + headless CMS → custom application) with a pick-by-organizational-capacity grid and a plugin-cost break-even analysis. Sits *between* `application-types/README.md` (what artifact?) and `website-builders.md` (which product?).

Insights worth codifying:

- **The three-article ladder: artifact → tier → product.** When a topic folder has an overview ("what kind of thing?") and a tooling survey ("which product?"), the *missing middle* is often the most valuable article: "how much machinery does this need, and who will run it?" The tooling survey answers a question the reader can only ask once the tier is settled. Signal that the middle article is missing: the survey's decision table keeps saying "it depends on your team" without a framework for that.
- **Decide on capacity, not requirements.** The article's load-bearing move is a 2-axis grid — *organizational capacity* (nobody technical / procedure-following / one technical person / a team) × *requirement shape* (publish / frequent multi-editor / governed / per-user state / is-the-product) — rather than a feature checklist. It makes the two classic errors legible as off-diagonal cells: engineers building brochureware in the product framework (bottom-left), and a no-capacity org committing to governed multi-role publishing on a builder (top-right). **Reusable wherever "which tool" really means "which tool can this org still run in three years."**
- **"Split the artifact before you upgrade the tier."** The most valuable single recommendation was structural, not comparative: a company with a marketing site *and* an invoice portal has two requirements, and building both at the higher tier means the marketing team files tickets forever. Say explicitly that most orgs should be on **more than one tier at once**, and that the question is never "what tier is my company" but "what tier is *this surface*."
- **A break-even is a shape, not a number.** Rather than a dollar crossover (which would be fake — see below), name three *signals*: paying for plugins to simulate a missing capability, a requirement whose answer is "Enterprise, contact sales," and human process standing in for a feature. Then price the concrete stack anyway (~$1,300 first year / ~$1,700 renewal in WordPress governance plugins) so the reader has an anchor. **And recommend deleting the requirement as a live option** — governance requirements are the most expensive and least examined.
- **The Enterprise-gated pair is a genuine cross-vendor finding.** Across Webflow, Squarespace, Hygraph, Contentful and Sanity, *editorial workflow* and *custom roles* are the two features held back for sales-quoted tiers — and they are exactly the two features that make an org outgrow a builder. Storyblok ($99/mo) was the only vendor selling approval workflows at a published self-serve price. **When the same two features are gated at the same tier across an entire category, that's the article's headline, not a footnote.**
- **Contaminated-source findings deserve a `[!CAUTION]`, not silence.** The "maintenance is 15–20% of build cost per year" rule of thumb returned eleven sources, **every one an agency selling custom development or maintenance retainers**, citing each other and unnamed "decades of enterprise data." Rather than repeat it or drop the topic, the article states the contamination as the finding, then substitutes something with real provenance — **Lientz & Swanson (1980)**, a 487-organisation survey, and uses its *shape* ("~60% of maintenance effort is adaptive/perfective, not corrective") instead of any percentage. Also flagged the common misquotation of that very statistic. **Pattern: when every source for a widely-repeated number is selling the thing the number justifies, say so, and find a claim whose shape survives without the number.**
- **Do not launder a user-asserted fact into a verified one.** The todo asserted `werdewechsel.de` is a Hakyll site. Research confirmed it is static, Netlify-hosted, 5,774 bytes of hand-shaped HTML with **no `<meta name="generator">`** and no public repo — *consistent with* Hakyll but not confirming it. The article attributes the claim to the author explicitly, lists what is independently verifiable, and states that consistency is not confirmation. **When a user supplies a fact from personal experience, cite it as theirs rather than either dropping it or restating it as verified** — their knowledge is the better source, but the provenance must be visible.
- **Deprecations can invalidate the standard recipe for a whole tier.** "Deploy Decap to Netlify, enable Identity + Git Gateway, invite the client by email" was *the* way to give a non-technical editor a git-CMS login. **Netlify now documents Git Gateway as deprecated with no recommended replacement.** Any tutorial written earlier still appears to work while building on deprecated infra. The current honest paths (Keystatic Cloud, Decap + DecapBridge) had to be surfaced with a `[!CAUTION]`. **When researching a well-known recipe, check the deprecation status of each moving part — the recipe outlives the infrastructure in blog posts.**
- **Cost-model divergence beats cost comparison.** For an identical 1M-pageview static site: **$0–5/mo on Cloudflare** (no egress metering at all), **~$85–105 on Vercel**, **~$195+ on Netlify**. Label derived arithmetic as derived. And name the real driver of bill shock — for Vercel it is **page count × revalidation frequency × function invocations**, not traffic, which is why a $200 estimate became $2,800 in a community report. **"Your bill is a function of your architecture, not your audience" is the transferable sentence.**
- **Migration difficulty belongs in its own table, and it changes the recommendation.** Rating each tier-to-tier move (1→3 is a rebuild because there's no export; 3→4 is trivial because content is already Markdown in git) produces the article's strongest single piece of advice: **Tier 3 is the best hedge, because it is both the cheapest to run and the cheapest to leave.** A survey that only compares capabilities cannot reach that conclusion.
